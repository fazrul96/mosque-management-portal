import {CALLBACK, DONATIONS, INBOX, LOGISTICS, PRAYER_TIMES, PROFILE, SLASH} from "./AppConstants.ts";

export const ROUTE_HOME: string = SLASH;
export const ROUTE_INBOX: string = SLASH + INBOX;

export const ROUTE_DONATIONS: string = SLASH + DONATIONS;
export const ROUTE_LOGISTIC: string = SLASH + LOGISTICS;
export const ROUTE_PRAYER_TIMES: string = SLASH + PRAYER_TIMES;

export const ROUTE_PROFILE: string = SLASH + PROFILE;
export const ROUTE_CALLBACK: string = SLASH + CALLBACK;